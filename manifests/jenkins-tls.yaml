---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jenkins-tls
  namespace: jenkins
spec:
  secretName: jenkins-tls
  issuerRef:
    name: cluster-internal-ca
    kind: ClusterIssuer
  commonName: jenkins.k3s.lan
  dnsNames:
    - jenkins.k3s.lan
